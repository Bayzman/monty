#include "monty.h"

/**
 * i_nop - does nothing on the satck
 * @head: the head of the stack
 * @counter: counter
 */

void i_nop(__attribute__((unused)) stack_t **head, unsigned int counter)
{
	(void) counter;
	;
}
